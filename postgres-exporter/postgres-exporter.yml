auth_modules:
  db1:
    type: userpass
    userpass:
      username: ${DB1_USER}
      password: ${DB1_PASSWORD}
    options:
      sslmode: require
      
  db2:
    type: userpass
    userpass:
      username: ${DB2_USER}
      password: ${DB2_PASSWORD}
    options:
      sslmode: require
      
  db3:
    type: userpass
    userpass:
      username: ${DB3_USER}
      password: ${DB3_PASSWORD}
    options:
      sslmode: require